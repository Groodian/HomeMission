import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#3f51b5',
    },
  },
  /*  myButton: {
    backgroundColor: "red",
    color: "white",
    border: "1px solid black",
  },*/
});
