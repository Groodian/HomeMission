import {
  User,
  TaskType,
  TaskSeries,
  Home,
  Task,
  TaskReceipt,
  History,
} from './index';

const entities = [User, TaskType, TaskSeries, Home, Task, TaskReceipt, History];

export default entities;
