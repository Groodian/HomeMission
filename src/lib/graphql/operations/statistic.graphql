query Statistics($start: Float!, $end: Float!) {
  homeStatistic(start: $start, end: $end) {
    data {
      date
      pointsDay
      pointsWeek
    }
  }
  userStatistics(start: $start, end: $end) {
    user {
      id
      name
      picture
    }
    data {
      date
      pointsDay
      pointsWeek
    }
  }
}
