query Home {
  home {
    id
    name
    code
    users {
      id
      name
      picture
    }
  }
}

mutation CreateHome($name: String!) {
  createHome(name: $name) {
    id
    name
    code
    users {
      id
      name
      picture
    }
  }
}

mutation JoinHome($code: String!) {
  joinHome(code: $code) {
    id
    name
    code
    users {
      id
      name
      picture
    }
  }
}
