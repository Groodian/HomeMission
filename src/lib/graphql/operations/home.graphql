query Home {
  home {
    id
    name
    code
  }
}

mutation CreateHome {
  createHome {
    id
    name
    code
  }
}

mutation JoinHome($code: String!) {
  joinHomeByCode(code: $code) {
    id
    name
    code
  }
}
